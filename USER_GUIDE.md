# 光伏电池板缺陷检测系统 - 使用指南

## 系统简介

光伏电池板缺陷检测系统是一个基于人工智能技术的自动化检测系统，能够快速、准确地识别太阳能电池板的各种缺陷，并生成专业的分析报告。系统采用现代化的全栈技术栈，包括前端、后端和AI服务。

**核心功能**：
- 图片上传检测
- YOLOv11 模型缺陷识别
- Kimi 2.5 LLM 缺陷分析
- 历史记录管理
- 数据可视化统计
- 用户认证和个人中心

## 系统访问

### 启动服务

**一键启动**：
1. 打开 `D:\project\data1` 目录
2. 双击运行 `start_all.bat` 文件
3. 等待所有服务启动完成

**手动启动**：
1. **后端服务**：
   ```bash
   cd D:\project\data1
   mvn spring-boot:run
   ```

2. **前端服务**：
   ```bash
   cd D:\project\data1\frontend
   npm run dev
   ```

3. **AI服务**：
   ```bash
   cd D:\project\data1\fastapi
   python main.py
   ```

### 访问地址

- **前端**：http://localhost:5173
- **后端 API**：http://localhost:8080
- **FastAPI**：http://localhost:8000

## 系统使用流程

### 1. 登录系统

1. 打开前端页面：http://localhost:5173
2. 点击「登录」按钮
3. 输入用户名和密码（默认：admin / 123456）
4. 点击「登录」按钮进入系统

### 2. 检测工作台

**图片检测**：
1. 点击左侧菜单「检测工作台」
2. 在「图片检测示例」标签页中，点击「点击上传」按钮
3. 选择一张光伏电池板的EL图像
4. 在顶部选择检测模型（默认：YOLOv11）
5. 点击「开始检测」按钮
6. 等待检测完成，查看检测结果和缺陷列表
7. 点击「分析缺陷」按钮，查看AI分析报告

**视频检测**（待实现）：
1. 点击「视频检测」标签页
2. 开启摄像头或上传视频文件
3. 捕获帧并进行检测

**摄像头检测**（待实现）：
1. 点击「摄像头检测」标签页
2. 开启摄像头
3. 开始实时检测

### 3. 历史记录

1. 点击左侧菜单「历史记录」
2. 在过滤条件中选择检测模式、模型版本和日期范围
3. 点击「查询」按钮查看符合条件的检测记录
4. 点击「查看」按钮查看检测记录详情
5. 点击「查看分析」按钮查看AI分析报告

### 4. 数据大盘

1. 点击左侧菜单「数据大盘」
2. 查看系统的统计数据，包括：
   - 总检测次数和本月新增
   - 总缺陷数量和本月新增
   - 检测准确率
   - 模型使用频率
   - 缺陷类型占比
   - 每日检测数量趋势
   - 模型使用频率
   - 检测模式分布

### 5. 个人中心

1. 点击左侧菜单「个人中心」
2. 查看个人信息
3. 修改个人信息（如姓名、头像等）
4. 点击「保存修改」按钮保存更改

## 缺陷类型说明

系统支持检测以下12种缺陷类型：

| 缺陷类型 | 描述 |
|---------|------|
| crack | 裂纹 |
| finger | 栅线 |
| black_core | 黑芯 |
| thick_line | 粗线 |
| star_crack | 星形裂纹 |
| corner | 边角缺陷 |
| fragment | 碎片 |
| scratch | 划痕 |
| horizontal_dislocation | 水平错位 |
| vertical_dislocation | 垂直错位 |
| printing_error | 印刷错误 |
| short_circuit | 短路 |

## 常见问题

### 1. 登录失败

**原因**：用户名或密码错误
**解决方法**：使用默认用户名 `admin` 和密码 `123456` 登录

### 2. 检测失败

**原因**：
- FastAPI 服务未启动
- YOLO 模型文件路径错误
- 图片格式不支持

**解决方法**：
- 确保 FastAPI 服务正在运行
- 检查 `yolo_service.py` 中的模型路径配置
- 使用 JPG 或 PNG 格式的图片

### 3. 分析报告生成失败

**原因**：Kimi LLM API 调用失败
**解决方法**：检查网络连接，确保能够访问 Kimi API

### 4. 历史记录查询失败

**原因**：数据库连接问题
**解决方法**：确保 H2 数据库文件存在且可访问

### 5. 数据大盘显示异常

**原因**：后端统计 API 调用失败
**解决方法**：检查后端服务是否正常运行

## 系统要求

### 硬件要求
- CPU：至少 8 核
- 内存：至少 16GB
- 存储：至少 500GB
- GPU：推荐 NVIDIA GPU（用于 YOLO 模型加速）

### 软件要求
- Java：OpenJDK 21
- Node.js：v18.17.0+
- Python：3.9+
- Maven：3.8.0+
- 浏览器：Chrome、Firefox、Edge 最新版本

## 联系方式

如有问题，请联系系统管理员。